apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: registration-service-db
  namespace: event-management
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-ssm
    kind: ClusterSecretStore
  target:
    name: registration-service-db
    creationPolicy: Owner
  data:
    - secretKey: DB_URL
      remoteRef:
        key: /event-management/registration-service/db-url
    - secretKey: DB_USERNAME
      remoteRef:
        key: /event-management/registration-service/db-username
    - secretKey: DB_PASSWORD
      remoteRef:
        key: /event-management/registration-service/db-password
