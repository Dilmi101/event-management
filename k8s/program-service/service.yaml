apiVersion: v1
kind: Service
metadata:
  name: program-service
  namespace: event-management
  labels:
    app: program-service
spec:
  selector:
    app: program-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
    - name: grpc
      port: 9092
      targetPort: 9092
