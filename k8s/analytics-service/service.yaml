apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: event-management
  labels:
    app: analytics-service
spec:
  selector:
    app: analytics-service
  ports:
    - name: http
      port: 8085
      targetPort: 8085
